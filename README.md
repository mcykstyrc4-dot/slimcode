# Slim Code Lab — одностраничный лендинг

Редизайн сайта сети салонов аппаратной коррекции фигуры «Slim Code Lab»
(LPG-массаж, RSL-скульптурирование, миостимуляция, прессомассаж, кавитация).

Лендинг — один файл **`index.html`**: HTML + CSS + JS, без сборки и фреймворков.
Открывается двойным кликом, заливается на любой хостинг как статика.

## Файлы

| Файл | Что это |
|---|---|
| `index.html` | лендинг целиком: разметка, стили и скрипты в одном файле |
| `privacy.html` | политика конфиденциальности (настоящий текст заказчика) |
| `terms.html` | пользовательское соглашение (настоящий текст заказчика) |
| `legal.css` | общие стили трёх юридических страниц |
| `assets/` | фирменный логотип, иконки и обложка для соцсетей |
| `assets/source/` | исходный макет логотипа, из которого собрано остальное |
| `tools/make-logo-assets.py` | пересобирает все файлы логотипа из исходника |
| `tools/make-photos.py` | готовит фото команды: AVIF/WebP/JPEG в двух размерах |
| `tools/make-prices.py` | собирает секции «Услуги» и «Акции» из прайса |
| `assets/team/` | фото специалистов в трёх форматах |
| `assets/hero-*.{avif,webp,jpg}` | фото первого экрана в трёх форматах |
| `telegram-proxy.php` | приём заявок и отправка в Telegram — для хостинга с PHP |
| `telegram-worker.js` | то же самое для Cloudflare Workers |
| `robots.txt` | правила обхода + ссылка на карту сайта |
| `sitemap.xml` | карта сайта |

`terms.html` и `privacy.html` содержат настоящие тексты заказчика и открыты
для индексации. Отдельной страницы с согласием на обработку данных нет —
её убрали по решению заказчика, согласие даётся через форму со ссылкой
на политику.

Политика собирается скриптом `tools/build-privacy.py` из текста, зашитого
в сам скрипт. Правки вносите там и запускайте `python3 tools/build-privacy.py`.

**Что в присланном тексте политики стоит проверить:**

- в пунктах 1.2, 2.3, 2.8, 2.10, 7.1 и 8.2 сайт указан как `slim-code.ru`,
  а в пункте 14.3 — `slimcodelab.ru`. Перенесено дословно, домены не сводил;
- пункт 5.3 говорит о Яндекс Метрике и Гугл Аналитике, но счётчиков на сайте
  сейчас нет — либо поставьте их, либо поправьте формулировку;
- в исходнике заголовки разделов 12 и 13 стояли местами: пункты 12.1-12.2
  про трансграничную передачу шли под заголовком «Конфиденциальность».
  Порядок восстановлен по нумерации самих пунктов.

**В документах два разных лица — так и задумано, подтверждено заказчиком:**

- **ИП Колодзей Татьяна Николаевна** — администрация сайта в пользовательском
  соглашении, она же оператор персональных данных в политике и согласии;
- **ООО «Слим Код»** — правообладатель и держатель медицинской лицензии
  Л041-01148-78/03316215, указан в копирайте.

Менять местами не нужно.

Реквизитов (ИНН, ОГРН, адрес) в документах нет: заказчик их не приводит.
Почта для обращений по персональным данным — kolodzey@mail.ru, указана
в самой политике.

Копирайт и номер медицинской лицензии стоят в подвале всех четырёх страниц.
Год окончания периода подставляет скрипт, вручную обновлять не нужно.

## Что внутри

| Блок | Якорь | Комментарий |
|---|---|---|
| Хиро | — | цельный заголовок, дата основания, одна основная CTA |
| Цифры доверия | — | 8000+ клиентов, 3 салона в 2 городах, счётчики анимируются |
| Услуги | `#services` | 5 карточек, первая (RSL) — флагманская, у каждой «узнать цену» |
| Акции | `#promos` | 3 карточки со скидками и сроками |
| Звёздные клиенты | `#stars` | видеоотзывы: карточка открывает ролик в окне |
| Команда | `#team` | 4 специалиста, фото 3:4 |
| Адреса | `#contacts` | 3 салона, телефон, WhatsApp |
| Футер | — | VK, Instagram, WhatsApp, копирайт, юридические ссылки |

Плюс модальное окно записи (подставляет услугу из нажатой кнопки) и плавающая
кнопка WhatsApp на мобильных.

### Раздел «О сети»

Письмо основателя дано клиентом целиком и оставлено дословно — правится только
типографика (кавычки-ёлочки, неразрывные пробелы, тире). Сразу видно
приветствие и первый абзац, остальное открывается по «Читать письмо целиком»
той же плавной анимацией, что и прайс: письмо длинное, развёрнутым оно
вытесняет со страницы всё остальное.

Рядом с письмом — четыре коротких факта (24 года, 8 мастеров, 3 лаборатории,
медицинская лицензия). Их читают те, кто письмо читать не станет, а таких
большинство.

Портрет обрамлён так же, как кадр первого экрана: рассеянная тень наружу и
лёгкое затемнение по краям, чтобы фотография уходила в фон. На большом экране
он остаётся на месте, пока листают письмо (`position:sticky`).

Основатель добавлена в микроразметку (`founder`), там же уточнена дата
основания — 1 октября 2001 года.

## Дизайн-решения

- **Цвет.** Глубина набирается оттенками, а не заливкой: тёмный винный
  (`--wine-950 … --wine-600`), кремовый текст (`--cream`), золото (`--gold`)
  на тонких деталях. Яркий красный (`--accent`) — только основные кнопки,
  бейджи акций и активная точка карусели.
- **Секции чередуются** тёмная / светлая — это и даёт ритм вместо сплошного алого.
- **Иерархия кнопок.** На экране одна залитая CTA; второстепенные действия —
  текстовые со стрелкой. Обводка есть только у кнопки WhatsApp.
- **Типографика.** Manrope (гротеск) + Cormorant Garamond (акцентная антиква),
  системные фолбэки на случай, если Google Fonts недоступны.
- **Mobile-first.** Базовые стили — мобильные, медиазапросы только «вверх»
  (600 / 900 / 1100 px).
- **Анимации** ненавязчивые: появление блоков при скролле, счётчики, ховеры.
  Полностью отключаются при `prefers-reduced-motion`.
- Без JS страница остаётся читаемой (контент виден, цифры на месте).

## Логотип и фирменный стиль

Логотип извлечён из присланного макета и разложен на варианты — все белые,
с прозрачным фоном, поэтому ложатся на любой тёмный фон:

| Файл | Где используется |
|---|---|
| `assets/logo.png` | шапка лендинга и внутренних страниц (знак + «Slim Code») |
| `assets/logo-full.png` | футер — то же плюс слоган «Стань лучшей версией себя» |
| `assets/logo-dark.png` | версия фирменным красным, для светлого фона |
| `assets/logo-mark.png` | только знак |
| `assets/favicon-32.png`, `assets/apple-touch-icon.png` | иконка вкладки и иконка на телефоне |
| `assets/og-cover.jpg` | превью при отправке ссылки в мессенджеры и соцсети |

Размеры в вёрстке заданы по высоте (`.logo img{height:…}`), пропорции считает
браузер — поменять масштаб можно одной строкой.

Все файлы собираются из `assets/source/logo-original.jpg` скриптом:

```
python3 tools/make-logo-assets.py
```

Надпись «Slim Code» набрана плотнее исходного макета — за это отвечает
константа `BOLD` в начале скрипта: `1` — как в макете, `3` — лёгкое
утолщение, `5` — текущее (+37% массы штриха), `7` — заметное. Знак
(силуэт и эллипс) не утолщается никогда: это рисунок, а не шрифт.

**Если есть исходник логотипа в векторе** (SVG, AI, EPS) — пришлите, заменю
растр на SVG: он будет чётким на любом экране и займёт пару килобайт вместо
шестнадцати. Сейчас качество ограничено присланным JPEG.

Фирменный красный с плашки логотипа — `#961F17`.

### Фирменный шрифт

В `index.html` подготовлен блок `@font-face` и токен `--brand` (ищите
комментарий «ФИРМЕННЫЙ ШРИФТ» в начале стилей). Чтобы подключить:

1. положите файлы в `assets/fonts/` — лучше всего `.woff2`;
2. раскомментируйте блок и поправьте имена файлов;
3. замените `--brand:var(--sans)` на `--brand:"SlimCode",var(--sans)`.

До этого момента `--brand` равен основному гротеску, и вёрстка не ломается.

## Данные: что уже настоящее, а что ещё нет

**Настоящее:** телефон, три адреса, ссылки на онлайн-запись Dikidi, WhatsApp,
Instagram, ВКонтакте и Telegram, фотографии и должности пяти специалистов,
весь прайс (услуги, пробные пакеты, комбинированные курсы), логотип, письмо
основателя и её фотография.

**Ещё нет:**

- Отзывов на самом сайте нет: они живут на Яндекс.Картах. На странице
  показана оценка 5,0 со ссылкой на карточку организации — в блоке
  контактов и на фотографии первого экрана. Знак «Хорошее место 2026»
  вынесен в самый верх, к плашке «с 2001»: это преимущество, его видно
  сразу при открытии сайта, в том числе с телефона. 2ГИС не используем.
  Год на знаке зашит в разметку: в следующем году не забудьте обновить.
  В микроразметку рейтинг намеренно не добавлен: `aggregateRating` в
  Schema.org рассчитан на отзывы, собранные на самом сайте, а разметка
  чужих оценок нарушает правила поисковых систем.
- **Приём заявок в Telegram** — бота нет, заявки идут в WhatsApp. Если бот
  появится, переключение — одна строка, см. раздел выше.
- **8000+ клиентов** в блоке цифр — из первоначального брифа. В письме
  основателя сказано «около 10 000 девушек и женщин»: цифры на одной странице
  расходятся, нужно выбрать одну. Поменять — строка `data-count` в блоке цифр.
- **8 мастеров** — так в письме основателя, а фотографий в разделе «Команда»
  пять. Пришлите ещё три (те же карточки из соцсетей), положу в
  `assets/source/team/` и пересоберу.
- **Дата подписи письма.** Текст написан к 1 октября 2025 года; когда сети
  исполнится 25, письмо и факт «24 года» надо обновить — они в разделе
  «О сети» в `index.html` и в заголовке `24 года`.

Часы работы на сайте не показываются нигде — ни в карточках салонов, ни в
микроразметке. Это осознанное решение по просьбе заказчика.

Все такие места помечены в коде комментарием `ЗАМЕНИТЬ`, а в юридических
страницах — подсвечены жёлтым (класс `.fill`).

### Как править цены

Цены лежат в одном месте — в структуре `PRICES` в `tools/make-prices.py`.
После правки запустите:

```
python3 tools/make-prices.py
```

Скрипт пересоберёт карточки услуг, полный прайс и блок с курсами. Руками
в `index.html` цены не правьте — при следующем запуске скрипт их перезапишет.

1. **Телефоны** — `+7 (812) 000-00-00` и три номера салонов (`tel:` тоже).
2. **WhatsApp** — `https://wa.me/78120000000` (4 места: шапка контактов, футер,
   плавающая кнопка).
3. **Адреса, метро, часы работы** — блок `#contacts`.
4. **Тексты услуг, цены и сроки акций** — блоки `#services` и `#promos`.
5. **Отзывы** — тексты, имена, инициалы в аватарах, даты.
6. **Фотографии** — исходники лежат в `assets/source/`. Чтобы заменить,
   положите новый файл на место старого и запустите
   `python3 tools/make-photos.py` — он пересоберёт все форматы и размеры.
8. **Соцсети** — в футере ВКонтакте, Instagram, Telegram и WhatsApp;
    ссылки на сообщества также в микроразметке (`sameAs`).
9. **Юридические страницы** — `privacy.html` и `terms.html`.
10. **Отправка формы** — блок `CONFIG` в начале скрипта, см. раздел ниже.
11. **Мета-теги** — `canonical` и домен,
    если он отличается от `slimcodelab.ru`.
12. **Микроразметка** — блок `application/ld+json` в `<head>`: реквизиты, адреса,
    телефоны, координаты салонов, часы работы, ссылки на соцсети.
    Рейтинг (`aggregateRating`) намеренно не размечен — его допустимо добавлять
    только по реальным подтверждённым отзывам.
13. **Юридические страницы** — реквизиты организации (ИНН, ОГРН, адрес), дата
    редакции, срок хранения данных, название системы аналитики.
    Тексты шаблонные: перед публикацией их должен посмотреть юрист.
    Не забудьте удалить памятку `.notice` в начале каждой страницы.
14. **`robots.txt` и `sitemap.xml`** — домен и дата `lastmod`.

## Куда уходит заявка

**Сейчас: WhatsApp, без сервера и бота.** Форма собирает имя, телефон, услугу
и салон, после чего открывает чат WhatsApp с уже набранным сообщением —
клиенту остаётся нажать «отправить». Ничего настраивать не нужно, номер задан
в `CONFIG.whatsapp` в начале скрипта.

Под формой есть и второй путь — прямые ссылки на онлайн-запись Dikidi для тех,
кто хочет выбрать время сам, не дожидаясь звонка.

### Если появится телеграм-бот

Достаточно указать `CONFIG.endpoint` — форма переключится на отправку на
сервер, а WhatsApp останется запасным вариантом при сбое. Токен бота **нельзя**
класть в код страницы: его увидит любой посетитель, и чат зальют спамом.
Поэтому заявка уходит на ваш обработчик, а он уже обращается к API Telegram.

### 1. Бот и чат

1. Создайте бота у [@BotFather](https://t.me/BotFather), получите токен
   вида `123456789:AA...`.
2. Напишите боту любое сообщение (или добавьте его в группу и напишите туда).
3. Откройте `https://api.telegram.org/bot<ТОКЕН>/getUpdates` и возьмите
   `result[0].message.chat.id` — это адрес, куда слать заявки.

### 2. Обработчик — один из двух

**Хостинг с PHP.** Положите `telegram-proxy.php`, например в `/api/lead.php`.
Токен и chat_id задайте переменными окружения `TG_BOT_TOKEN` и `TG_CHAT_ID`
(или впишите в начале файла). В `index.html`:

```js
CONFIG.endpoint = '/api/lead.php';
```

**Cloudflare Workers** (бесплатного тарифа хватает). Разверните
`telegram-worker.js`, укажите свой домен в `ALLOWED_ORIGINS` и задайте секреты:

```
npx wrangler secret put TG_BOT_TOKEN
npx wrangler secret put TG_CHAT_ID
```

В `index.html` пропишите адрес воркера в `CONFIG.endpoint`.

Оба обработчика: проверяют имя и телефон, режут длину полей, чистят
управляющие символы, молча отбрасывают ботов и **собирают текст сообщения
сами** — присланный страницей текст не используется, иначе через форму
можно было бы отправить в ваш чат произвольное содержимое.
PHP-версия дополнительно не принимает больше одной заявки с адреса в 15 секунд.

## Отзывы звёздных клиентов

Секция строится из списка `STARS` в начале скрипта в `index.html`. Для каждого
отзыва нужны:

```js
{ id: 'dQw4w9WgXcQ', name: 'Имя Фамилия', role: 'актриса' },
```

- `id` — код ролика из ссылки YouTube. Из `https://youtu.be/dQw4w9WgXcQ`
  или `https://www.youtube.com/watch?v=dQw4w9WgXcQ` берём `dQw4w9WgXcQ`;
- `name` — имя, оно же подпись под обложкой и в окне с видео;
- `role` — чем известна; можно оставить пустую строку.

**Пока список пуст, секция удаляется со страницы целиком** — пустого блока
не остаётся, так что незаполненный список ничего не ломает.

Обложки берутся с YouTube автоматически. Если пришлёте свои фотографии,
добавьте в запись `cover: 'assets/stars/имя.jpg'` — она перекроет обложку
с YouTube. Если обложка не загрузилась, карточка показывает тёмную подложку
с кнопкой воспроизведения, а не битую картинку.

Ролик подгружается только при открытии окна: до этого на странице нет ни
одного кадра с YouTube и его скриптов. Используется домен `youtube-nocookie.com`.
При закрытии плеер выгружается, иначе звук продолжал бы играть.

## Счётчики статистики

Метрика и Аналитика включаются номерами в `CONFIG` в начале скрипта
в `index.html` — вставлять код со страниц сервисов не нужно:

```js
metrika: '',   // номер счётчика Яндекс Метрики, например 87654321
ga:      '',   // идентификатор ресурса GA4, например G-AB12CD34EF
```

Где взять номера, если счётчики уже работают на старом сайте:

- **Метрика** — metrika.yandex.ru, номер написан в списке счётчиков под
  названием сайта. Тот же номер, а не новый: иначе история посещений
  оборвётся и сравнить старый сайт с новым не получится.
- **GA4** — analytics.google.com, **Администратор → Потоки данных**, поле
  «Идентификатор потока» вида `G-XXXXXXXXXX`.

Пока поля пустые, наружу не уходит ни одного запроса — счётчиков на странице
просто нет.

### Цели

Счётчики сами по себе показывают только посещаемость. Полезные цифры — сколько
человек оставили заявку и как они это сделали. Страница отправляет в оба
сервиса события:

| Событие | Когда |
| --- | --- |
| `lead` | заявка из формы отправлена |
| `whatsapp` | нажата любая кнопка WhatsApp |
| `telegram` | переход в Telegram |
| `booking` | переход в Dikidi на онлайн-запись |
| `call` | нажат номер телефона |
| `maps` | переход в карточку Яндекс.Карт |

В Метрике их нужно один раз завести: **Настройка → Цели → Добавить цель →
JavaScript-событие**, идентификатор — имя из таблицы. В GA4 события приходят
сами, отмечать ключевыми — в разделе «События».

### Почему теги грузятся после загрузки страницы

Оба счётчика подключаются после события `load` и в фоне. Так они не отнимают
канал у первого экрана — а посетителей с телефонов почти 90 %. На точность
статистики это не влияет: уйти со страницы раньше, чем она догрузится, почти
никто не успевает.

### Что ещё стоит знать

- Счётчик живёт в коде страницы, поэтому при замене старого сайта на новый он
  исчезнет вместе со старым — номер надо перенести руками (см. выше).
- Пункт 5.3 политики конфиденциальности уже называет Метрику и Аналитику,
  отдельно дописывать ничего не нужно.
- Тег `<noscript>` с картинкой-пикселем Метрики не ставим: он нужен только для
  посетителей с отключённым JavaScript, а таких единицы.

### Бизнес-мессенджер Jivo

Виджет подключается одной строкой — идентификатором в `CONFIG.jivoId`
в начале скрипта в `index.html`:

1. заведите аккаунт на jivo.ru и добавьте сайт;
2. в личном кабинете откройте **Управление → Каналы связи → Установить код**;
3. в выданном коде вида
   `<script src="//code.jivo.ru/widget/AbCdEf1234" async></script>`
   возьмите последнюю часть адреса — `AbCdEf1234`;
4. впишите её в `CONFIG.jivoId` и опубликуйте сайт.

Сам код вставлять в разметку не нужно, страница подключит виджет сама.

**Почему не просто вставить код с сайта Jivo.** Виджет весит несколько сотен
килобайт и открывает свои соединения. Вставленный «как есть», он тормозит
первый экран, а у сети почти все посетители с телефонов. Поэтому скрипт
грузится отложенно: при первом действии посетителя (касание, прокрутка,
клавиша) либо через 8 секунд — что случится раньше. Человек всё равно не
успевает дойти до кнопки чата быстрее.

**Кнопка WhatsApp.** Jivo ставит свою кнопку в правый нижний угол — туда же,
где наша плавающая кнопка WhatsApp. Как только виджет загрузился, своя кнопка
прячется, чтобы они не наезжали друг на друга. Если хотите оставить обе,
уберите строку `fab.style.display = 'none'` и сдвиньте `.fab` вверх.

**Политика конфиденциальности.** Jivo собирает данные посетителей и ставит
свои cookie. Пункт 5.3 политики упоминает только Яндекс Метрику и Гугл
Аналитику — стоит дописать туда Jivo.

### Крайний случай

Можно заполнить `CONFIG.telegram.token` и `CONFIG.telegram.chatId` — страница
обратится к API Telegram напрямую. **Токен при этом виден всем**, кто откроет
исходный код страницы: его смогут использовать для спама в ваш чат, и токен
придётся отзывать. Годится только как временное решение.

### Что уже сделано в форме

- маска телефона `+7 (999) 123-45-67`: понимает ввод с 8, вставку из буфера,
  правку в середине номера и Backspace по разделителям;
- проверка имени и полноты номера с подсказками под полями;
- состояние отправки на кнопке и защита от двойного клика;
- при сбое отправки на сервер — сообщение с телефоном и ссылкой на WhatsApp;
- скрытое поле-ловушка и отсечка мгновенных отправок: заявки ботов не уходят
  ни на сервер, ни в Telegram.

## Проверено

Хром (headless) на 390×844 и 1440×900: горизонтального скролла нет, ошибок в
консоли нет, работают меню, модалка с валидацией, карусель, счётчики, плавающая
кнопка; страница читается с отключённым JS.

Юридические страницы: горизонтального скролла нет, все ссылки оглавления ведут
к существующим разделам, внутренние ссылки между страницами не битые.
JSON-LD разбирается как валидный JSON, `sitemap.xml` — как валидный XML.

Форма (19 проверок в браузере): маска на всех сценариях ввода, валидация,
успешная отправка с разбором ушедшего запроса, поведение при ошибке сервера,
отсечка ботов. Оба обработчика прогнаны на локальной заглушке Telegram API
(14 проверок для воркера, 6 для PHP): валидные заявки доходят, некорректные
отклоняются с понятным кодом, чужой домен и GET не принимаются.
